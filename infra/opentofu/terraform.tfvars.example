project_id = "application-review-platform"
# Runtime sizing and disk values may be overridden in an ignored local tfvars file.
# Owner/admin IAM principals belong only in infra/opentofu/bootstrap/*.tfvars.
