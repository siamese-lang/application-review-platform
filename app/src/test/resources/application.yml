spring:
  datasource:
    url: ${TEST_DB_URL:jdbc:postgresql://localhost:5432/application_review_test}
    username: ${TEST_DB_USERNAME:postgres}
    password: ${TEST_DB_PASSWORD:postgres}
  jpa:
    hibernate:
      ddl-auto: validate
