---
- name: Set timezone
  community.general.timezone: { name: "{{ timezone }}" }
- name: Install baseline packages
  ansible.builtin.apt:
    name: [ca-certificates, curl, gnupg, jq, python3]
    state: present
    update_cache: true
